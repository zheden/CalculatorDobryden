void makeConnectedTree(vector <TreeNode*>& io_tree);
double calculateTree(const vector <TreeNode*>& i_tree);
int intFromString(const string& i_str);
TreeNode* strGetNum(const string& i_str, size_t& i_numPos, bool& io_nextislast);
TreeNode* strGetPos(const string& i_str, size_t& i_numPos);
void makeNodesVector(const string& i_str, vector<TreeNode*>& io_vect);
